--- Expose any globals here, typically just your control file if you want to access it through rcon or commands
-- luacheck: globals clusterio_trains
clusterio_trains = require("modules/clusterio_trains/control")
