--- Can contain anything you want to allow other plugins to have access to, this example exposes the control file
-- Access the exports from other modules using require("modules/clusterio_trains")
return require("modules/clusterio_trains/control")
